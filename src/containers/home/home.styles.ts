import styled from 'styled-components';

export const Button = styled.button`
  margin: 16px 0px;
  min-width: 100px;
  padding: 10px 16px;
  height: 44px;
  border: none;
  border-radius: 4px;

  cursor: pointer;
  transition: 0.5s;

  &:hover {
  }
`;
